import TwitchEventProcessor from './TwitchEventProcessor';

describe('The Twitch Event Processor', () => {
  test('has coverage', () => {
    // eslint-disable-next-line no-new
    new TwitchEventProcessor();
  });
});
